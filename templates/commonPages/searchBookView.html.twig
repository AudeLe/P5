{% extends "template.html.twig" %}

{% block title %}Recherche d'un livre - {{ session.login }}{% endblock %}

{% block content %}
    <div class="row">
        {% block navbarLeftSide %}
            {% if session.status == 'admin' %}
                {% include 'adminPages/navbarAdmin.html.twig' %}
            {% else %}
                {% include 'memberPages/navbarMember.html.twig' %}
            {% endif %}
        {% endblock %}

        {% block displayContent %}
            <div class="col-9">

                {% block profileBanner %}
                    <div class="col-lg-12" class="generalProfileBanner">

                        {% include 'commonPages/profileBanner.html.twig' %}

                        <div class="col-lg-12 profileBannerText">
                            <h3>Chercher un livre</h3>
                        </div>

                    </div>
                {% endblock %}

                <div class="col-lg-12">
                    <p>Texte à insérer</p>

                    {% block searchBook %}
                        <h4>Chercher si l'on a déjà un livre</h4>
                        <form action="../public/index.php?action=searchBook&id={{ session.id }}" method="post">
                            <div>
                                <label for="ISBNSearch">ISBN</label>
                                <input type="text" id="ISBNSearch" name="ISBNSearch" required/>
                            </div>
                            <div>
                                <input type="submit" value="Rechercher"/>
                            </div>
                        </form>

                        <div>
                            {{ message }}
                        </div>
                    {% endblock %}
                </div>
            </div>
        {% endblock %}
    </div>
{% endblock %}